/**
 * Shared API base URL helper.
 *
 * All environments: API is served from the same origin as the frontend
 * at /api/... — no proxy.php needed when the backend serves static files.
 */
export const API_BASE = "/api";
