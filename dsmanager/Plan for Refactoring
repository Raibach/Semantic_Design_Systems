# Repo Evaluation + Refactor Plan

## Part 1 — Findings (the deception you suspected is real)

I diffed the code against the actual A2UI v0.9.1 spec you pasted. The docs claim compliance; the code doesn't deliver it.

### A2UI compliance — claimed vs. actual

| Claim (docs/changelog) | Reality |
|---|---|
| "Zero-trust: catalog loaded at startup, all 4 return points validated" (2026-07-22) | **Gone.** Nothing loads `component-catalog.json` anywhere in backend or frontend. `validate_a2ui_component` doesn't exist. Likely lost in the 2026-07-27 "assembly restored" session. |
| "v0.9.1 compliant" | Envelope says `"version": "v0.9"`. No `deleteSurface`, no `action` client→server messages, no JSON Pointer data binding (`DynamicString`/path), no client-side functions (`required`, `formatString`…), no `sendDataModel` sync. |
| "Declarative JSON, not executable code" | `A2UISurfaceContainer.tsx:142` runs `eval()` on AI-supplied button onclick, plus raw `innerHTML` injection. Direct spec violation + XSS hole. The spec's `openUrl` section explicitly mandates scheme allowlists for this reason. |
| "Pre-approved component catalog (14 components)" | `assemble-surface` emits `ConsoleCardGrid`, `DecisionDialog`, `ActionGroup`, `variant:"greeting"` — **none exist in the catalog**. Frontend ignores the component tree anyway: `WritingAreaIndex` hand-parses the envelope and sets React state. There is no generic adjacency-list renderer (the spec's core component model). |
| "routers/ — each file is included in main.py via include_router()" (`routers/__init__.py`) | **False.** `main.py` never calls `include_router`. The entire `routers/` package (8 files) is dead code — stale duplicates of endpoints main.py implements inline. |

### Dead code inventory (backend)

- **`backend/routers/` (8 files)** — dead, never wired in. `api_core.py` (20 lines) exists only to serve them.
- **Dead modules with no live importer:** `keeper_api.py`, `quarantine_api.py`, `debug_api.py`, `grace_gui_real.py` (196 lines), `mock_server.py`.
- **Dead endpoints in `main.py`:** `GET /api/ai/assemble-console` (L2708), `GET /api/ai/assemble-session/{id}` (L2839), `GET+POST /api/ai/assemble-composer` (L2999) — ~385 lines. Frontend calls **only** `/api/ai/assemble-surface`.
- **Hygiene in `main.py` (4,428 lines):** `import json` twice (L1/L4), `import os` duplicated at L4425, **Sentry DSN hardcoded** at L19, mid-file import of `figma_service` at L3999, duplicate `elapsed_ms` assignments still at L3381/L3392 (changelog claims removed), docstring describes an envelope shape the code doesn't return.
- **Frontend:** `lit/` folder contains React leftovers (`AiManagedContainer.tsx`, `StatusIndicatorReact.ts`); tag-registry has 40+ Zod schemas, ~5 have Lit implementations; `WritingAreaIndex.tsx` is 1,989 lines hand-rolling what a generic renderer should do.
- **Docs rot:** the "Verification (Retrospective)" block is copy-pasted identically into 5 changelog entries; `A2UI_V09_COMPLIANCE.md` describes a system that no longer exists.

## Part 2 — The Plan

**Phase 0 — Specifications reference (your request).** Create `SPECIFICATIONS.md` at repo root (same level as `CHANGELOG.md`) with three sections: **§1 A2UI Protocol v0.9.1** (full spec you pasted), **§2 Basic Catalog Implementation Guide v0.9.1**, **§3 A2UI Extension for A2A v0.9.1**. Verbatim, for reference.

**Phase 1 — `main.py` dead-code refactor (starting point).**
1. Delete `backend/routers/` + `api_core.py` (dead; we'll rebuild routers properly in Phase 4).
2. Delete `keeper_api.py`, `quarantine_api.py`, `debug_api.py`, `grace_gui_real.py`, `mock_server.py` (each re-verified with a final import grep before deletion).
3. Delete the three legacy assembly endpoints (~385 lines) after a final caller check.
4. Fix hygiene: dedupe imports, move Sentry DSN → `SENTRY_DSN` env var, hoist `figma_service` import, remove duplicate `elapsed_ms`, correct the `assemble-surface` docstring.
5. Verify: backend boots, `/api/health` + `/api/ai/assemble-surface` (all 3 intents) respond.

**Phase 2 — Restore actual A2UI v0.9.1 compliance.**
1. Restore catalog loading + server-side validation of every `updateComponents` payload against `component-catalog.json` (the spec's prompt→generate→validate loop, with `VALIDATION_FAILED` retry back to the LLM).
2. `"version": "v0.9.1"` everywhere; align `catalogId` with the catalog's `$id`.
3. Remove the non-spec `surface` key; remove `eval()`/`innerHTML` from `A2UISurfaceContainer` (retire the old tag-command path or sandbox it to catalog actions only).
4. Bring emitted components into the catalog (add `ConsoleCardGrid` + decision dialog components properly, or emit spec-native `List`/`Modal`/`Button` trees).

**Phase 3 — Generic renderer (frontend).** Build the spec's component model: flat component map → adjacency-list tree → render, with JSON Pointer data binding against the data model. Replaces the hand-parsed `if (surface === 'console')` logic in `WritingAreaIndex.tsx`. This is your Phase 2 from `A2UI_IMPLEMENTATION_PLAN.md`.

**Phase 4 — Docs + modularization.** Rewrite stale MDs against reality, clean the changelog's duplicated verification blocks, then split `main.py`'s remaining ~4,000 lines into proper domain routers.

**Execution order:** Phase 0 → 1 → verify → 2 → 3 → 4. Phases 0–1 are safe and immediate; Phase 2+ changes runtime behavior and I'll checkpoint with you before each.

### Decisions I need from you
1. **Dead files:** hard-delete (git preserves history) or move to an `attic/` folder first?
2. **Scope for this session:** Phases 0–1 only (specs + main.py dead-code purge), or continue into Phase 2 (compliance restoration) in the same run?

If the plan looks good, toggle to Act mode and I'll start with Phase 0 (`SPECIFICATIONS.md`) then the `main.py` purge.